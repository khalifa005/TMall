// tslint:disable-next-line:eofline
let data = 2 ;

// data = "2" ;

let data2;

data2 = 'data' ;

data2 = 23 ;

let fixedData: any ;

interface ICar {
    color: string;
    model: string;
    topSpeed?: number;
}

const car1: ICar = {
    color: 'blue',
    model: 'bmw',
};


const multiplie = (x: number, y: number): number =>
{
   return x * y ;
};
